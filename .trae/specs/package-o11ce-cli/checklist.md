- [x] `pip install -e .` instala el paquete y `o11ce --help` funciona
- [x] `o11ce up` levanta el stack vía Docker Compose o falla con error claro si Docker no está disponible
- [x] `o11ce down` detiene el stack sin borrar datos del usuario
- [x] Se crea directorio local de trabajo con `.env` y volúmenes (workspace/logs/cache/tmp)
- [x] No se imprimen ni loguean secretos (`OPENROUTER_API_KEY` ni env vars con KEY/TOKEN/SECRET)
